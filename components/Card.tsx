import styled from 'styled-components'
export const Card = styled.div`
  padding: 20px;
  border: 1px solid rgba(0, 0, 0, .95);

  *:first-child {
    margin-top: 0;
  }

  *:last-child {
    margin-bottom: 0;
  }
`